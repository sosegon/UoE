using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteInEditMode]
public class Left_Toe_Joint_01 : MonoBehaviour {


	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void FixedUpdate () {
		// if(ground_script.hit_by_mouse) {
		// 	Vector3 global_pos = ground_script.global_position_pointer;
		// 	transform.position = global_pos;
		// }
	}

	void Update() {
		// sphere_script = GameObject.Find("Sphere").GetComponent<Sphere>();

		// Vector3 global_pos = sphere_script.global_position_pointer;
		// Debug.Log("position" + global_position_pointer.ToString());

		// Vector3 pos = GameObject.Find("Sphere").transform.position;
		// transform.position = pos;
	}
}
